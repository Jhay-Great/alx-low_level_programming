#include "main.h"
/**
  * _isupper - function prototype
  * @c: parameter
  * Description: uppercase character checker
  * Return: 0
  */
int _isdigit(int c)
{
	if (c >= 0  && c <= 9)
		return (1);
	else
		return (0);
}
