#include "main.h"
/**
  * _strlen - function prototype
  * @s: is a parameter
  * Description: function to count the length of a string
  * Return: count.
  */
int _strlen(char *s)
{
	int count;

	for (count = 0; s[count] != '\0'; count++)
	{

	}
	return (count);
}
